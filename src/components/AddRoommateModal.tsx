import { useState, type FormEvent } from 'react'
import { Icon } from './Icon'
type Props = { onClose: () => void; onAdd: (name: string) => void }
export function AddRoommateModal({ onClose, onAdd }: Props) { const [name, setName] = useState(''); const submit = (event: FormEvent) => { event.preventDefault(); if (name.trim()) onAdd(name.trim()) }; return <div className="modal-backdrop" role="presentation" onMouseDown={onClose}><form className="modal" onMouseDown={(event) => event.stopPropagation()} onSubmit={submit}><button className="modal-close" type="button" onClick={onClose}><Icon name="close" /></button><p className="eyebrow">MAPLE HOUSE</p><h2>Add a roommate</h2><p>They will be added to the weekly chore rotation.</p><label htmlFor="roommate-name">Name</label><input autoFocus id="roommate-name" value={name} onChange={(event) => setName(event.target.value)} placeholder="e.g. Priya Sharma" /><button className="add-button" type="submit"><Icon name="plus" />Add roommate</button></form></div> }
