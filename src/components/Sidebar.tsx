import type { CurrentUser } from '../api'
import { Icon } from './Icon'
type Props = { roommateCount: number; activeView: 'board' | 'history'; user: CurrentUser | null; onViewChange: (view: 'board' | 'history') => void; onNotify: (message: string) => void; onFeedback: () => void; onLogout: () => void }
export function Sidebar({ roommateCount, activeView, user, onViewChange, onNotify, onFeedback, onLogout }: Props) { const initials = user?.name.split(/\s+/).map((part) => part[0]).slice(0, 2).join('').toUpperCase() ?? 'U'; return <nav className="sidebar"><div className="brand"><span className="brand-mark">r</span><span>roomie</span></div><div className="nav-links"><button className={`nav-link ${activeView === 'board' ? 'active' : ''}`} onClick={() => onViewChange('board')}><span className="nav-icon">⊞</span>Chore board</button><button className={`nav-link ${activeView === 'history' ? 'active' : ''}`} onClick={() => onViewChange('history')}><span className="nav-icon">◷</span>History</button></div><div className="home-card"><div className="home-illustration">⌂</div><strong>Maple House</strong><span>{roommateCount} roomies</span><button onClick={() => onNotify('Home settings opened')}>Manage home <Icon name="chevron" /></button></div><div className="sidebar-bottom"><button className="nav-link" onClick={onFeedback}><span className="nav-icon">?</span>Help & feedback</button><button className="profile" onClick={() => onNotify('Profile menu opened')}><span className="avatar" style={{ background: '#cfa2d5' }}>{initials}</span><span><strong>{user?.name ?? 'Loading…'}</strong><small>{user?.email ?? ''}</small></span><Icon name="chevron" /></button><button className="nav-link logout-button" onClick={onLogout}><span className="nav-icon">↪</span>Log out</button></div></nav> }
