import { useState, type FormEvent } from 'react'
import { Icon } from './Icon'
type Props = { type: 'feedback' | 'help'; onClose: () => void; onSend: (message: string, type: 'feedback' | 'help') => Promise<void> }
export function FeedbackModal({ type, onClose, onSend }: Props) { const [message, setMessage] = useState(''); const [busy, setBusy] = useState(false); const submit = async (event: FormEvent) => { event.preventDefault(); if (!message.trim()) return; setBusy(true); await onSend(message.trim(), type); setBusy(false); onClose() }; return <div className="modal-backdrop" onMouseDown={onClose}><form className="modal" onMouseDown={(event) => event.stopPropagation()} onSubmit={submit}><button className="modal-close" type="button" onClick={onClose}><Icon name="close" /></button><p className="eyebrow">MAPLE HOUSE</p><h2>{type === 'help' ? 'How can we help?' : 'Share feedback'}</h2><p>Your message will be saved for the house manager.</p><label htmlFor="feedback-message">Message</label><textarea id="feedback-message" value={message} onChange={(event) => setMessage(event.target.value)} placeholder="Write your message…" required /><button className="add-button" disabled={busy}>{busy ? 'Sending…' : 'Send message'}</button></form></div> }
